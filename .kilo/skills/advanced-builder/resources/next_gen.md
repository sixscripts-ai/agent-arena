# Next-Gen Idea Bank (DO NOT IMPLEMENT unless asked)

Level5 Frontend Code: WRITE frontend/src/pages/... , EXEC pnpm build, pnpm check, Playwright screenshot

Level6 Appwrite DB Write: DB_CREATE/READ scoped by battle_id, auto-cleanup, wrapper in InternalClient

Level7 Self-Evolve: WRITE executors/formats/my_evo.py + EXEC pytest, AST validation (no os.system rm -rf), human review queue, ELO for executors

Combined: builder builds FastAPI+React+Appwrite schema, breaker uses full toolbelt+frontend+DB+proposes improved executor

Keep judge simple 0-100.
